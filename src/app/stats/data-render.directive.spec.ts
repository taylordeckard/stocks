import { DataRenderDirective } from './data-render.directive';

describe('DataRenderDirective', () => {
  it('should create an instance', () => {
    const directive = new DataRenderDirective();
    expect(directive).toBeTruthy();
  });
});
