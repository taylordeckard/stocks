import { FilterHighlightedPipe } from './filter-highlighted.pipe';

describe('FilterHighlightedPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterHighlightedPipe();
    expect(pipe).toBeTruthy();
  });
});
