import { StockColorPipe } from './stock-color.pipe';

describe('StockColorPipe', () => {
  it('create an instance', () => {
    const pipe = new StockColorPipe();
    expect(pipe).toBeTruthy();
  });
});
